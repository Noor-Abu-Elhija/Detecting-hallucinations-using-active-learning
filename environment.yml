name: project
channels:
  - defaults
  - conda-forge
  - pytorch
  - nvidia
dependencies:
  - python=3.10
  - numpy<2.0      # <-- Downgraded numpy
  - pandas
  - scikit-learn
  - tqdm
  - pyyaml
  - sentencepiece
  - pyarrow
  # GPU PyTorch stack for older GPUs (CUDA 11.7)
  - pytorch=2.0.1
  - torchvision=0.15.2
  - torchaudio=2.0.2
  - pytorch-cuda=11.7
  - pip
  - pip:
      - sentence-transformers==2.7.0 # <-- Downgraded
      - transformers==4.38.2        # <-- Downgraded
      - accelerate==0.25.0          # <-- Downgraded
      - tf-keras
      - faiss-cpu
      - python-dotenv
      - tiktoken
      - datasets
      - "huggingface_hub[hf_xet]"