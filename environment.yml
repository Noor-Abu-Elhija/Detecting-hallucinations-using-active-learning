name: project
channels:
  - defaults
  - conda-forge
  - pytorch
  - nvidia            # ← add this for CUDA toolkit
dependencies:
  - python=3.10
  - numpy
  - pandas
  - scikit-learn
  - tqdm
  - pyyaml
  - sentencepiece
  - pyarrow
  # GPU PyTorch stack (pick CUDA version that matches `nvidia-smi`, 12.1 is common):
  - pytorch
  - torchvision
  - torchaudio
  - pytorch-cuda=12.1
  - pip
  - pip:
      - sentence-transformers>=2.2.2
      - transformers>=4.38.2
      - tf-keras
      - faiss-cpu
      - python-dotenv
      - accelerate
      - tiktoken
      - datasets
      - "huggingface_hub[hf_xet]"
